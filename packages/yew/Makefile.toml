[tasks.native-test]
command = "cargo"
args = ["test", "--features", "csr,test"]

[tasks.wasm-test]
command = "wasm-pack"
args = [
    "test",
    "--firefox",
    "--headless",
    "--",
    "--features",
    "csr,test",
]

[tasks.test]
args = ["test", "--all-targets", "--all-features"]
dependencies = ["native-test", "wasm-test"]

[tasks.clippy-feature-soundness]
command = "cargo"
args = [
    "hack", "clippy",
    "-p", "yew", "-p", "yew-agent", "-p", "yew-router",
    "--feature-powerset", "--no-dev-deps",
    "--keep-going",
    "--", "-D", "warnings",
]
