# Tests Archive

This branch does not keep archived browser-only test sources in-tree.

The `tests-archive/` directory is retained as a placeholder for Lynx-specific
test migration notes or future archived tests. Add files here only when they
are needed by the Lynx branch.
